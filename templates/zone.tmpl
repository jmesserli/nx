;
; BIND zone generated by netbox-to-bind (https://github.com/jmesserli/netbox-to-bind)
; Start of zone {{ .ZoneName }}
;

{{ with .SOAInfo -}}
$TTL	{{ .BindDefaultRRTTL }}
@	IN SOA	{{ .NameserverFQDN }} {{ .DottedMailResponsible }} (
	{{ .Serial }}	; Serial
	{{ .Refresh }}	; Refresh
	{{ .Retry }}	; Retry
	{{ .Expire }}	; Expire
	{{ .TTL }}	; Negative Cache TTL
)

; Nameserver
@ NS {{ .NameserverFQDN }}
{{- end }}

; Name	Type	RData
{{ range $rr := .Records -}}
    {{ $rr.Name }}	{{ $rr.Type }}	{{ $rr.RData }}
{{ end }}
;
; End of zone {{ .ZoneName }}
; Generated at {{ .GeneratedAt }}
;
